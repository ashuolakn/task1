version: "3.9"  # optional since v1.27.0
services:
  web:
    build: .
    ports:
      - "80:80"
  deploy:
    resources:
      reservations:
        cpu: 300m
        memory: 500Mi
      limits:
        cpu: 500m
        memory: 800Mi
